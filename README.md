# URL Shortener con FastAPI
Proyecto para practicar el desarrollo de APIs y persistencia de datos.

## Tecnologías usadas:
- Python 3.x
- FastAPI
- SQLAlchemy (SQLite)

## Cómo ejecutar:
1. Crear venv: `python -m venv venv`
2. Instalar dependencias: `pip install -r requirements.txt`